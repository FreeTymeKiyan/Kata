// main class of kata mergesort
class Main {

    static int[] array = {17, 
        16,
        17,
        17,
        18,
        3,
        20,
        1,
        2,
        16,
        11,
        17,
        17,
        5,
        7,
        11,
        18,
        1,
        20,
        10
    };

    public static void main(String args[]) {
        printArr(array);  
        //MergeSort.sort(array, 0, array.length - 1);
        //MergeSort1.sort(array, 0, array.length - 1);
        //MergeSort2.sort(array, 0, array.length - 1);
        //MergeSort3.sort(array, 0 , array.length - 1);
        //MergeSort4.sort(array, 0, array.length - 1);
        //MergeSort5.sort(array, 0, array.length - 1);
        //MergeSort6.sort(array, 0, array.length - 1);
        //MergeSort7.sort(array, 0, array.length - 1);
        //MergeSort8.sort(array, 0, array.length - 1);
        //MergeSort9.sort(array, 0, array.length - 1);
        //MergeSort10.sort(array, 0, array.length - 1);
        //MergeSort11.sort(array, 0, array.length - 1);
        //MergeSort12.sort(array, 0, array.length - 1);
        //MergeSort13.sort(array, 0, array.length - 1);
        //MergeSort14.sort(array, 0, array.length - 1);
        //MergeSort15.sort(array, 0, array.length - 1);
        //MergeSort16.sort(array, 0, array.length - 1);
        //MergeSort17.sort(array, 0, array.length - 1);
        //MergeSort18.sort(array, 0, array.length - 1);
        //MergeSort19.sort(array, 0, array.length - 1);
        //MergeSort20.sort(array, 0, array.length - 1);
        //MergeSort21.sort(array, 0, array.length - 1);
        //MergeSort22.sort(array, 0, array.length - 1);
        //MergeSort23.sort(array, 0, array.length - 1);
        //MergeSort24.sort(array, 0, array.length - 1);
        //MergeSort25.sort(array, 0, array.length - 1);
        //MergeSort26.sort(array, 0, array.length - 1);
        //MergeSort27.sort(array, 0, array.length - 1);
        //MergeSort28.sort(array, 0, array.length - 1);
        //MergeSort29.sort(array, 0, array.length - 1);
        //MergeSort30.sort(array, 0, array.length - 1);
        //MergeSort31.sort(array, 0, array.length - 1);
        //MergeSort32.sort(array, 0, array.length - 1);
        // MergeSort33.sort(array, 0, array.length - 1);
        // MergeSort34.sort(array, 0, array.length - 1);
        MergeSort35.sort(array, 0, array.length - 1);
        printArr(array);
    }

    private static void printArr(int[] array) {
        for(int i = 0; i < array.length; i++) {
            System.out.print(array[i] + ",");
        }    
        System.out.println();
    }
}
